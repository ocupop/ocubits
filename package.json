{
  "name": "@ocupop/ocubits",
  "version": "0.1.27",
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "scripts": {
    "lerna:build": "npx lerna run build",
    "lerna:clean": "npx lerna clean -y",
    "lerna:graph": "npx nx graph"
  },
  "workspaces": [
    "packages/*"
  ]
}
